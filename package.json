{
  "name": "talent-city-marketplace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MVP биржи городских задач и талантов",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node --check src/server.js && node --check src/db.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=24"
  }
}
